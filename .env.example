# Register an OAuth 2 application at
# https://www.openstreetmap.org/oauth2/applications with the scopes
# "read_prefs" and "write_api", and a redirect URI of
# http://localhost:8000/callback for local development.
OSM_CLIENT=
OSM_SECRET=
